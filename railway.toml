[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

# KasmVNC on port 8080
[[deploy.healthchecks]]
port = 8080
path = "/"
initialDelaySeconds = 60
timeoutSeconds = 15
periodSeconds = 30
