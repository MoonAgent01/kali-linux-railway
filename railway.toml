[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

# KasmVNC serves HTTPS on port 6901
[[deploy.healthchecks]]
port = 6901
path = "/"
initialDelaySeconds = 60
timeoutSeconds = 15
periodSeconds = 30
